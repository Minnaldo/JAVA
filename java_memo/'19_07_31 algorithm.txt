190731

- ()()((()))
- ((()((((()()((()())((())))
문자열을 입력받는다.

:문자열을 입력받는다.
문자열을 한 글자씩 탐색해가면서 열림괄호를 만나면 스택에 때려받는다.
닫힘괄호를 만나면 스택에서 하나 빼버린다.

중간에 EmptyStack상황에서 괄호를 빼야하는 빼야되는 상황이 되던가,
모두 끝났는데 스택이 비어있지 않다면 오류!